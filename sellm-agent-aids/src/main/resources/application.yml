spring:
  application:
    name: agent-aids
  cloud:
    nacos:
      discovery:
        server-addr: ${SELLM_NACOS_ADDR:127.0.0.1:8848}
server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health
