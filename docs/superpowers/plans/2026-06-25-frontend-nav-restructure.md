# 计划:前端导航重构(三平台 × 四角色)+ 顶栏简化

> 分支 feature/special-edu-llm,worktree dev_workspace,纯前端改动(后端已支持)。
> 本期范围(已确认):**先重排导航**。改个人信息/批量导入导出/机构增删/跨用户聚合管理 → 后续。

## 决策(已确认)
- 范围:导航容器重排 + 顶栏简化 + 按四角色分层 + 训练计划查看入口(后端已支持的部分)。
- 智能体平台 5 个 agent **按权限显示**(无权角色不显示该入口)。
- 教师康复全流程(多模态诊断/量表评估/评估报告/IEP/训练对比)**归智能体平台**(康复智能体下)。
- 批量导入导出 **后续做**。
- 一次改完提交。

## 现状关键事实(调研结论)
- 角色 getter 齐全:auth.js 的 isSuperAdmin/isManager/isTeacher/isParent。
- 后端行级权限完备:children/iep/training/teaching 的 list 已按角色或 ownerId 过滤,导航重排无需改后端鉴权。
- 现有 20 个页面 + list API 齐全(listIepsByChild/listCyclesByChild/listContents/listFamilyIepsByChild 等)。
- 顶栏现有"特殊教育评估助手 · 管理端"文字 + 用户名/角色/机构 + 退出。
- 缺失(本期不做,标注后续):改个人信息(无页无接口)、批量导入导出(全缺)、机构编辑/删除(仅有创建+列表)、跨用户产出聚合页(机构/平台看本机构教师产出,缺 orgId 聚合接口)。
- 注:项目只有 4 级角色,无"游客"角色;需求中"普通游客:改密码"→ 落为「改密码对全部已登录角色开放」。

<!-- PLACEHOLDER -->

## 顶栏简化
- 去掉左侧"特殊教育评估助手 · 管理端"文字。
- 顶栏保留:用户名 + 角色 tag + 机构(orgLabel) + 「首页」按钮 + 「退出登录」按钮。
- (左侧 aside 顶部 brand"特殊教育垂直大模型"保留,作平台标识。)

## 三平台 × 四角色 菜单矩阵

### 平台一:智能体平台(标题"智能体平台")
| 菜单项 | 路由 | PARENT | TEACHER | MANAGER | SUPER_ADMIN |
|---|---|:-:|:-:|:-:|:-:|
| 🤖 问答机器人 | /qa | ✓ | ✓ | ✓ | ✓ |
| 📚 教学训练 | /teaching | | ✓ | | ✓ |
| 🩺 康复智能体 | /diagnosis | | ✓ | | ✓ |
| 🧩 智能教具 | /aids | | ✓ | | ✓ |
| 🔬 教研科研 | /research | | ✓ | | ✓ |

康复智能体全流程(归智能体平台,TEACHER/SUPER_ADMIN;作为"康复智能体"的子流程缩进或紧邻):
多模态诊断 /diagnosis · 量表评估 /assessment · 评估报告 /report · 个别化教育计划 /iep · 训练对比评估 /training
家长侧康复产出:家庭 IEP /family-iep(PARENT)。

### 平台二:数据管理平台(标题"数据管理平台")
| 菜单项 | 路由 | PARENT | TEACHER | MANAGER | SUPER_ADMIN | 说明 |
|---|---|:-:|:-:|:-:|:-:|---|
| 儿童档案 | /children | ✓ | ✓ | ✓ | ✓ | 后端行级:家长自家/教师本班/机构本校/平台全部 |
| 训练计划管理 | /family-iep(家长) /training(教师) | ✓家长 | ✓教师 | | | 家长看自家家庭IEP;教师看自己落库的训练/IEP/教学产出 |
| 量表库管理 | /scale-library | | | | ✓ | 平台管理员 |

> 说明:机构/平台管理员"管理本机构教师产出"需 orgId 聚合接口(后端缺),本期**仅挂现有按 child/owner 维度的页**;机构/平台的跨用户产出聚合入口标注【后续】不挂或挂占位。儿童档案的数据范围已由后端 AccessGuard 按角色过滤,前端同一入口即可。

### 平台三:用户管理平台(标题"用户管理平台")
| 菜单项 | 路由 | PARENT | TEACHER | MANAGER | SUPER_ADMIN | 说明 |
|---|---|:-:|:-:|:-:|:-:|---|
| 用户中心(改密码) | /users | ✓ | ✓ | ✓ | ✓ | 改密码现有,全角色;改个人信息【后续】 |
| 家长审核 | /users | | ✓ | | | 教师审核分派给自己的家长(现有) |
| 班级管理 | /classes | | | ✓ | | 机构管理员(现有 CRUD;批量导入导出【后续】) |
| 教师/康复师管理 | /users | | | ✓ | | 机构管理员创建管理(现有 createUser;批量【后续】) |
| 机构与机构管理员 | /users | | | | ✓ | 平台创建机构含管理员(现有);编辑/删除机构【后续】 |
| 全部用户管理 | /users | | | | ✓ | 平台管理各级用户(现有编辑/软删/初始化密码;批量【后续】) |

> UsersView 已是"一页三态按角色分支"(超管/机构/教师各一套 UI + 通用改密卡),本期导航把它归入"用户管理平台";页面内部 UI 不重构(仅后续做改个人信息/批量时再动)。

## 实施(纯前端,改 3-4 个文件)
1. **MainLayout.vue 顶栏**:删"特殊教育评估助手 · 管理端",加「首页」按钮(router.push('/dashboard')),保留用户名/角色/机构/退出。
2. **MainLayout.vue 左侧导航**:重构为三个 el-menu-item-group:
   - 「智能体平台」:5 agent(按权限 v-if)+ 康复全流程子项(TEACHER/SUPER_ADMIN)+ 家庭IEP(PARENT)。
   - 「数据管理平台」:儿童档案(全角色)+ 训练计划管理(家长/family-iep,教师/training)+ 量表库(SUPER_ADMIN)。
   - 「用户管理平台」:按角色显示 用户中心/家长审核/班级管理/机构管理/全部用户(均指向现有 /users、/classes)。
   - 首页 /dashboard 顶部独立项保留。
3. 路由不变(全部指向现有页);后端不动。
4. (可选)给后续功能留菜单占位时用 disabled + "敬请期待" tooltip;本期默认**不挂未实现项**,避免点出 404/403。

## 验证
- npm run build 通过。
- 人工核对四角色登录后菜单符合矩阵(可用 dev 种子 admin + 临时建 teacher/manager/parent 核对,或仅 build + 代码审查 v-if 条件)。

## 不做(本期外,后续专项)
- 改个人信息(前端页 + 后端 getMe/updateMyProfile)。
- 批量导入导出(xlsx 依赖 + 后端 /import /export 批量接口,用户/儿童/班级/教师)。
- 机构编辑/删除(PUT/DELETE /api/orgs/{id})。
- 机构/平台"管理本机构教师产出"聚合页(后端 orgId 聚合接口)。
- 路由角色守卫(meta.roles + beforeEach;目前靠菜单隐藏 + 后端兜底)。

## 风险
- 菜单项多,v-if 角色条件要逐项核对,避免越权显示(后端有兜底,但菜单不该露不可用项)。
- "训练计划管理"家长/教师指向不同路由,用 v-if 分流。
- 需求里部分功能(改个人信息/批量/机构增删)本期不实现,菜单不挂,需向用户说明这些是后续。

