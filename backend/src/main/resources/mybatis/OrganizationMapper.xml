<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sellm.org.OrganizationMapper">

    <resultMap id="orgMap" type="map">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="region" property="region"/>
        <result column="disorder_types" property="disorderTypes"/>
        <result column="province" property="province"/>
        <result column="city" property="city"/>
    </resultMap>

    <insert id="insert" parameterType="map" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO organization (name, region, disorder_types, province, city)
        VALUES (#{name}, #{region}, #{disorderTypes}, #{province}, #{city})
    </insert>

    <select id="findById" parameterType="long" resultMap="orgMap">
        SELECT id, name, region, disorder_types, province, city FROM organization WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="orgMap">
        SELECT id, name, region, disorder_types, province, city FROM organization ORDER BY id
    </select>

</mapper>

